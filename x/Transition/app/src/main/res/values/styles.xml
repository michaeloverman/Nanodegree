<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="toolbarStyle">@style/Toolbar</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowSharedElementEnterTransition">@transition/changebounds</item>
        <item name="android:windowSharedElementExitTransition">@transition/changebounds</item>
    </style>

    <!--<item name="android:windowExitTransition">@transition/grid_exit</item>
    <item name="android:windowEnterAnimation">@transition/grid_exit</item>-->

    <!-- Main Activity Toolbar style -->
    <style name="Toolbar" parent="Widget.AppCompat.Toolbar">
        <item name="popupTheme">@style/Theme.AppCompat.Light</item>
    </style>

    <!-- Main activity theme. -->
    <style name="MovieTheme" parent="AppTheme">
        <item name="actionBarStyle">@style/ActionBar.Solid.Sunshine.NoTitle</item>
    </style>

    <!-- main activity action bar styles -->
    <style name="ActionBar.Solid.Sunshine.NoTitle" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="displayOptions">useLogo|showHome</item>
        <item name="logo">@drawable/ic_launcher</item>
    </style>

    <style name="TitleAppearance">
        <item name="android:typeface">sans</item>
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Large</item>
    </style>

    <!-- Define android:paddingEnd and Start in the layout, since it requires Min API level 17 -->
    <style name="InLayoutPadding">
        <item name="android:paddingTop">@dimen/margin_default</item>
        <item name="android:paddingRight">@dimen/margin_default</item>
        <item name="android:paddingLeft">@dimen/margin_default</item>
        <item name="android:paddingBottom">@dimen/margin_default</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="android:textAppearance">@style/MyCustomTabText</item>
        <item name="tabSelectedTextColor">@color/white</item>
        <item name="tabIndicatorColor">@color/white</item>
    </style>

    <style name="MyCustomTabText" parent="TextAppearance.AppCompat.Button">
        <item name="android:textSize">12sp</item>

        <item name="android:textColor">@color/grey300</item>
        <item name="textAllCaps">true</item>
        <!--<item name="android:paddingLeft">4dp</item>-->
    </style>

    <style name="Theme.App.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/grey500</item>
        <item name="colorControlActivated">@color/grey500</item>
        <item name="colorControlHighlight">@color/grey500</item>
    </style>
    
    <style name="Dialog.Theme" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
